const max_username_length = 30;
const min_username_length = 4;

const max_message_length = 500;
const min_message_length = 1;

const max_password_length = 20;
const min_password_length = 4;

const logo_path = "assets/logo.png";
const logout_path = "assets/logout.png";

const app_version = "1.1.0";

const logo_image = document.getElementById("logo-image");
logo_image.src = logo_path;

const login_logo_image = document.getElementById("login-logo-image");
login_logo_image.src = logo_path;

const logout_image = document.getElementById("logout-image");
logout_image.src = logout_path;